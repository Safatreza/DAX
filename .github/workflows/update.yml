name: RSL DAX 40 Update

on:
  schedule:
    - cron: '0 10,22 * * 1-5'  # Weekdays at 10:00 UTC (pre-market EU) and 22:00 UTC (post-close)
  workflow_dispatch:

jobs:
  update-dax-data:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run RSL DAX 40 screening
        run: python run_dax_live.py
        env:
          PYTHONUNBUFFERED: "1"

      - name: Validate output (require ≥ 35 stocks)
        run: |
          python - <<'EOF'
          import json, sys, os
          path = 'web/data/dax_rankings.json'
          if not os.path.exists(path):
              print("ERROR: JSON file not found — run_dax_live.py may have crashed.")
              sys.exit(1)
          with open(path) as f:
              data = json.load(f)
          total = data['metadata']['total_analyzed']
          failed = data['metadata']['failed']
          print(f"Analyzed: {total}, Failed: {failed}")
          if total < 35:
              print(f"ERROR: only {total} stocks analyzed — aborting commit.")
              sys.exit(1)
          EOF

      - name: Commit updated data
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add web/data/dax_rankings.json
          git diff --cached --quiet || git commit -m "chore: DAX 40 RSL update $(date +'%Y-%m-%d %H:%M UTC')"
          git push

      - name: Trigger Vercel deployment
        continue-on-error: true
        env:
          VERCEL_DEPLOY_HOOK_URL: ${{ secrets.VERCEL_DEPLOY_HOOK_URL }}
        run: |
          if [ -n "$VERCEL_DEPLOY_HOOK_URL" ]; then
            curl -X POST "$VERCEL_DEPLOY_HOOK_URL"
          else
            echo "No VERCEL_DEPLOY_HOOK_URL set — Vercel deploys automatically from the git push."
          fi
